---
// AboutHero.astro — Hero banner for the About page
---

<section class="about-hero">
  <div class="about-hero__mobile-logo">
    <img src="/images/Main-Logo-KM-Law.webp" alt="KM Law" class="about-hero__logo-img" />
  </div>
  <div class="about-hero__bottom">
    <h1 class="about-hero__headline">We Make Complex Issues Manageable.</h1>
    <a href="#about-content" class="about-hero__link">
      Learn More
      <svg class="about-hero__link-arrow" width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M1 13L13 1M13 1H3M13 1V11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </a>
  </div>
</section>

<style>
  .about-hero {
    width: 100%;
    height: 520px;
    background: #1B3B49;
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    padding: 0 max(80px, calc((100vw - 1600px) / 2));
    box-sizing: border-box;
  }

  .about-hero__tagline-row {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    padding: 24px 0;
    gap: 8px;
    width: 100%;
    border-bottom: 1px solid #78716C;
    margin-top: 148px;
  }

  .about-hero__tagline {
    font-family: 'Poppins', sans-serif;
    font-weight: 500;
    font-style: italic;
    font-size: 20px;
    line-height: 140%;
    letter-spacing: 0.01em;
    color: #1B3B49;
  }

  .about-hero__bottom {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: flex-end;
    width: 100%;
    flex-grow: 1;
    padding-bottom: calc(40px + 4rem);
    max-width: 1600px;
  }

  .about-hero__headline {
    width: 690px;
    font-family: 'Poppins', sans-serif;
    font-weight: 400;
    font-size: 70px;
    line-height: 110%;
    letter-spacing: -0.03em;
    color: #FFFFFF;
  }

  .about-hero__link {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    padding: 0 16px;
    gap: 24px;
    border-bottom: 1px solid #FFFFFF;
    width: fit-content;
    text-decoration: none;
    font-family: 'Poppins', sans-serif;
    font-weight: 500;
    font-size: 14px;
    line-height: 21px;
    letter-spacing: -0.02em;
    color: #FFFFFF;
    flex-shrink: 0;
    transition: color 0.3s ease;
  }

  .about-hero__link:hover {
    color: rgba(255, 255, 255, 0.6);
  }

  .about-hero__link-arrow {
    width: 14px;
    height: 14px;
    color: #FFFFFF;
    display: inline-block;
    flex-shrink: 0;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .about-hero__link:hover .about-hero__link-arrow {
    transform: translate(3px, -3px);
  }

  /* Mobile logo — hidden on desktop */
  .about-hero__mobile-logo {
    display: none;
  }

  .about-hero__logo-img {
    max-width: 150px;
    max-height: 100px;
    object-fit: contain;
    mix-blend-mode: darken;
  }

  /* ---- Animations ---- */
  @keyframes aboutFadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .about-hero__tagline-row {
    animation: aboutFadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.3s both;
  }

  .about-hero__headline {
    animation: aboutFadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.5s both;
  }

  .about-hero__link {
    animation: aboutFadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.7s both;
  }

  /* ---- Responsive ---- */
  @media (max-width: 1200px) {
    .about-hero { padding: 0 60px; }
    .about-hero__headline { font-size: 60px; width: 580px; }
  }

  @media (max-width: 1024px) {
    .about-hero { padding: 0 40px; }
    .about-hero__headline { font-size: 52px; width: 500px; }
  }

  @media (max-width: 768px) {
    .about-hero {
      padding: 0 24px;
      height: auto;
      min-height: 380px;
      justify-content: flex-end;
    }
    .about-hero__mobile-logo {
      display: none;
    }
    .about-hero__bottom {
      flex-direction: column;
      align-items: flex-start;
      gap: 24px;
      flex-grow: 0;
      padding-top: 24px;
      padding-bottom: 32px;
    }
    .about-hero__headline {
      font-size: 30px;
      width: 100%;
    }
  }

  @media (max-width: 480px) {
    .about-hero { padding: 0 16px; min-height: 340px; }
    .about-hero__mobile-logo { padding-top: 80px; }
    .about-hero__bottom { padding-top: 20px; }
    .about-hero__headline { font-size: 26px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .about-hero__tagline-row,
    .about-hero__headline,
    .about-hero__link {
      animation: none;
    }
  }
</style>

<script>
  // Re-bind on every Astro view-transition swap so the smooth-scroll handler
  // attaches to the freshly mounted DOM. astro:page-load also fires on first load.
  function initAboutHeroScroll() {
    const learnMoreLink = document.querySelector<HTMLAnchorElement>('.about-hero__link');
    if (!learnMoreLink) return;

    learnMoreLink.addEventListener('click', function(e) {
      e.preventDefault();
      const href = learnMoreLink.getAttribute('href');
      if (!href) return;
      const targetEl = document.getElementById(href.substring(1));
      targetEl?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  }

  document.addEventListener('astro:page-load', initAboutHeroScroll);
</script>
